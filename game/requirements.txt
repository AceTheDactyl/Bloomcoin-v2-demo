# BloomQuest Dependencies
# Install with: pip3 install -r requirements.txt

# Required for Web Interface only (optional)
flask>=2.0.0
flask-socketio>=5.0.0

# Optional but recommended
colorama>=0.4.0  # Better cross-platform color support
